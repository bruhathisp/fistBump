import type { TableProps, Row, ColumnSchema } from "./types";
declare class Table<T extends ColumnSchema> {
    private props;
    constructor(props: TableProps<T>);
    getAllRows(): Promise<Row<T>[]>;
}
interface AppProps {
    id: string;
    token?: string;
}
declare class App {
    private props;
    constructor(props: AppProps);
    table<T extends ColumnSchema>(props: Omit<TableProps<T>, "app">): Table<T>;
}
export declare function app(props: AppProps): App;
export declare function table<T extends ColumnSchema>(props: TableProps<T>): Table<T>;
export {};
